Depose ici, sur ta machine, avant de lancer ../install.sh :

  1) Ton installeur Flash CS6 :
       - soit l'ISO : local/Flash Professional CS6.iso  (le script la montera)
       - soit le dossier extrait contenant Set-up.exe

  2) Ton amtlib.dll :
       local/amtlib.dll

Ces fichiers sont gitignores : ils ne seront JAMAIS commites ni pousses sur GitHub.
Le depot ne les fournit pas.
